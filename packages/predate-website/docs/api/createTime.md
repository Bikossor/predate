# `createTime`

Creates a new `PreTime` from the given parameters.

```ts
function createTime(time: {
  hours: number;
  minutes: number;
  seconds?: number;
  milliseconds?: number;
}): PreTime;
```
