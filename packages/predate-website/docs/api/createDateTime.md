# `createDateTime`

Creates a new `DateTime` from the given parameters.

```ts
function createDateTime(params: {
  year: number;
  month: number;
  day: number;
  hours: number;
  minutes: number;
  seconds?: number;
  milliseconds?: number;
}): DateTime;
```
