# `createDateTime`

Creates a new `PreDateTime` from the given parameters.

```ts
function createDateTime(params: {
  year: number;
  month: number;
  day: number;
  hours: number;
  minutes: number;
  seconds?: number;
  milliseconds?: number;
}): PreDateTime;
```
